jsonschema>=4.0.0
PyYAML>=6.0
# --- LLM SDKs (all optional) -------------------------------------------------
# The base demo runs with MockProvider only. Each vendor SDK below is lazy-
# imported and only needed if you select that provider via examples or the
# multi-vendor eval harness. Install only what you need.
anthropic>=0.40.0
# google-generativeai>=0.7.0   # for compliance_gate.providers.GeminiProvider
# openai>=1.30.0               # for compliance_gate.providers.OpenAIProvider
